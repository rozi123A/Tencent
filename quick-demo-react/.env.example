# SDKAppID is not secret and can safely live in the client build.
VITE_SDK_APP_ID=your_numeric_sdk_app_id

# URL of the backend signing server (see /server) that generates userSig.
# The SDKSecretKey lives ONLY on that server, never in this frontend build.
VITE_USER_SIG_SERVER_URL=http://localhost:3001
