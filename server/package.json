{
  "name": "trtc-usersig-server",
  "version": "1.0.0",
  "private": true,
  "description": "Minimal backend that generates TRTC userSig server-side so SDKSecretKey never reaches the browser.",
  "main": "index.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "tls-sig-api-v2": "^1.0.2"
  }
}
