# Copy to .env (and add .env to .gitignore — never commit real secrets)
TENCENT_SDK_APP_ID=your_numeric_sdk_app_id
TENCENT_SDK_SECRET_KEY=your_sdk_secret_key
PORT=3001
ALLOWED_ORIGIN=http://localhost:5173
